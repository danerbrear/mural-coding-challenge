{
  "name": "mural-marketplace-backend",
  "version": "1.0.0",
  "description": "Marketplace backend for Mural Pay coding challenge",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run build:lambda",
    "build:lambda": "tsc && npm run copy:node_modules",
    "copy:node_modules": "cp -r node_modules dist/ 2>/dev/null || xcopy /E /I node_modules dist\\node_modules 2>nul || true",
    "package": "npm run build:lambda && cd dist && zip -r ../lambda.zip . && cd ..",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "ts-lambda-api": "^2.5.3",
    "reflect-metadata": "^0.2.2",
    "uuid": "^9.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
